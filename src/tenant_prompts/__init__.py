"""Tenant prompt templates (sub-proyek I).

Three rows seeded: ``lang_detect_input``, ``lang_detect_output``, ``translate``.
The repository is process-cached because templates change rarely; tests can
call :func:`clear_template_cache` to reset.
"""
